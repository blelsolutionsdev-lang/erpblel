import { EmConstrucao } from '@/components/EmConstrucao'

export function Movimentacoes() {
  return (
    <EmConstrucao
      title="Movimentações de estoque"
      description="Histórico de entradas, saídas, ajustes e transferências. As movimentações já existem no banco e ficam sincronizadas automaticamente com o estoque atual de cada produto — falta só a tela de consulta e lançamento manual."
    />
  )
}
